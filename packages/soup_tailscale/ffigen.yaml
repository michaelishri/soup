# Run with `dart run ffigen --config ffigen.yaml`.
name: SoupTailscaleBindings
description: |
  Bindings for `src/soup_tailscale.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/soup_tailscale_bindings_generated.dart'
headers:
  entry-points:
    - 'src/soup_tailscale.h'
  include-directives:
    - 'src/soup_tailscale.h'
ffi-native:
comments:
  style: any
  length: full
