name: soup_identity
description: Hand-rolled Soup Identity HTTP client for device-link, sessions, and roster.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  http: ^1.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0
