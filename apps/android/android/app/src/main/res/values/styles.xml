<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="TvTextInputTheme" parent="@android:style/Theme.Material.Light.Dialog.Alert">
        <item name="android:colorAccent">#2541C8</item>
        <!-- Google TV supplies dark defaults even for light dialog themes;
             define both foreground and background colours explicitly. -->
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:colorBackground">@color/soup_icon_background</item>
        <item name="android:colorForeground">#1C2027</item>
        <item name="android:textColorPrimary">#1C2027</item>
        <item name="android:textColorSecondary">#515867</item>
        <item name="android:textColorHint">#515867</item>
        <item name="android:colorButtonNormal">#E8F35B</item>
        <item name="android:colorControlNormal">#7C8391</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/TvTextInputButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/TvTextInputButton</item>
    </style>
    <style name="TvTextInputButton" parent="@android:style/Widget.Material.Light.Button">
        <item name="android:textColor">#2541C8</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/soup_icon_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/soup_launcher</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/soup_icon_background</item>
    </style>
</resources>
